const test = require('ava');

test('It should create an instance which you can then use to access the state', () => {

})

test('It should create an instance which you can then update the state via some function', () => {
  
})

test('It should create multiple instances with separate state each', () => {
  
})

test('It should reuse the functions in each instance', () => {
  
})
